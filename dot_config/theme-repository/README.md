# Theme Repository
This directory contains color scheme definitions in .txt format.
The active theme name is written dynamically to current.txt at runtime.
Client apps (like WezTerm and Sketchybar) load current.txt to apply the active theme.
